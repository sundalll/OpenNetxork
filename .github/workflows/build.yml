name: Build iOS App (.ipa) & Deploy Server

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-ipa:
    name: Build iOS .ipa Package
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify Swift & Xcode
        run: |
          xcodebuild -version
          swift --version

      - name: Create .app Bundle & Compile Binary
        run: |
          mkdir -p Payload/OpenNetwork.app
          cp iOS/Info.plist Payload/OpenNetwork.app/Info.plist
          printf "APPL????" > Payload/OpenNetwork.app/PkgInfo
          
          # Compile Swift source files into executable binary for iOS
          swiftc -v -target arm64-apple-ios14.0-simulator \
            -sdk $(xcrun --sdk iphonesimulator --show-sdk-path) \
            iOS/Models/*.swift iOS/Services/*.swift iOS/ViewModels/*.swift \
            iOS/Views/Common/*.swift iOS/Views/Feed/*.swift iOS/Views/Profile/*.swift \
            iOS/Views/Music/*.swift iOS/Views/Video/*.swift iOS/Views/Channels/*.swift \
            iOS/Views/Auth/*.swift iOS/Views/MainTabView.swift iOS/App/SocialNetworkApp.swift \
            -parse-as-library -emit-executable \
            -o Payload/OpenNetwork.app/OpenNetwork

      - name: Package .ipa File
        run: |
          zip -r OpenNetwork.ipa Payload/
          ls -lh OpenNetwork.ipa

      - name: Upload .ipa Artifact to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: OpenNetwork-iOS-IPA
          path: OpenNetwork.ipa
